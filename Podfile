source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '10.0'
use_frameworks!

target 'mobile-ios-grid-image-async-loading' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mobile-ios-grid-image-async-loading
  pod 'Kingfisher', '~> 6.0'

  target 'mobile-ios-grid-image-async-loadingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'mobile-ios-grid-image-async-loadingUITests' do
    # Pods for testing
  end

end
